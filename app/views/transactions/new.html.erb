<h1>Initial Amount</h1>

<h2>Please enter the amount of money you want to deposit.</h2>

<%= form_for :transaction, url: "transaction/amount" do |f| %>
    Amount: <%= f.number_field :amount %> 
    <%=f.collection_select(:account_id, Account.all, :id, :name,  {prompt: "Select Account"})%>
    <%= f.submit 'Submit' %>

<% end %>