<h1>Balance</h1>

<h2>Please select account to see his current balance! </h2>


<%= form_for :transaction, url: '/balance' do |f| %>
    <%=f.collection_select(:account_id, current_user.accounts, :id, :name, {:prompt => "Select Account"})%>
    <%= f.submit 'Submit' %>



<% end %>